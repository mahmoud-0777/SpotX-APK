# Supported Spotify versions

Current tested compatibility targets:

- 8.9.x
- 9.0.x
- 9.1.x

If patch signatures change, update patch definitions in `patches/patches.json` and profile mappings in `config/profiles/*.json`.
